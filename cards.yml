build:
  version: "cards-local"

contact-info:
  email:
    support: "cards-local.1"
    general: "cards-local.1"
  phone:
    support: "cards-local.1"
    general: "cards-local.1"
  mail: "cards-local.1"
